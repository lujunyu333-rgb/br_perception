# 雷达感知层容器化重构 — 任务清单

> 2026-09-14 · 配套设计文档：`doc/容器化重构设计_20260914.md`（**先读它**）
> 执行环境：Ubuntu 22.04 + ROS 2 Humble，目标平台 GMKtec R5 6600H（6C12T）

---

## 〇、给执行者的总则（**每个任务开始前都要看**）

### 红线 —— 不许碰

| 项 | 原因 |
|---|---|
| `config/field_geometry.yaml` 的几何数据 | **场地唯一事实源**，经过 SolidWorks 线稿实测 + 逐条人工确认。重构只改代码，不改场地数据 |
| `scripts/field_model_generator.py` | 它的 `--apply` **会直接覆写** `config/field_geometry.yaml`，冲掉上述回填结果。**不要运行** |
| `scripts/field_model_spec.py` 里的 `SPOTS_*` / `RED_*` / `DIVIDER_*` / `SKY_GRID_*` | 旧 DRAFT 占位数据，**不是数据源**（只有 `COLORS` / `ZONE_COLORS` 还在用） |
| QoS 设计 | 见下方"已知坑"第 1 条，2026-09-14 刚修好，不要动 |

### 构建前置

```bash
source /opt/ros/humble/setup.bash
source ~/ws_livox/install/setup.bash      # ⚠ 必须: livox_ros_driver2 是 REQUIRED 依赖
source ~/lio_ws/install/setup.bash
cd ~/br_perception && colcon build --symlink-install
```

### 已知坑（踩过的，别再踩）

1. **QoS 兼容性** —— 2026-09-14 全仓库修过一轮。规则：
   - **订阅端一律 `rclcpp::SensorDataQoS()`**（BEST_EFFORT）。RELIABLE 订阅 ↔ BEST_EFFORT 发布 = 静默收不到数据，且**不报错**。
   - **发布端保持 `SystemDefaultsQoS()` / RELIABLE 不动** —— RELIABLE 发布对两种订阅者都兼容；改成 BEST_EFFORT 反而会踢掉别人的 RELIABLE 订阅（Point-LIO 可能就是）。
   - 出现"话题在、节点在、就是没数据"，**第一个怀疑 QoS**。
2. **仿真雷达垂直 FOV = ±14.9°**，与实机 Mid-360 的 −7°~+52° 不同。桥接节点 `pointcloud2_to_livox_custom` 的 `elev_min_deg`/`elev_max_deg` 已按仿真值配置，别照搬实机参数。
3. **Gazebo 改模型后必须整个重启**，不热重载。
4. **仿真雷达实际发布率 ~33Hz**（SDF 里的 `update_rate: 10` 对 CPU `ray` 不生效），实机 10Hz。
5. **本仓库是"仓库根 = ROS 包"**（`CMakeLists.txt` / `package.xml` 在根目录），不是 `src/<pkg>` 结构。
6. Ubuntu 上**只有 `python3`，没有 `python`**。

### 两条实现并存的事实（本次重构要解决的核心）

- 单体 `src/lidar/lidar_perception_node.cpp`（1570 行）**自己实现了全套算法**，没有 include 任何子模块头
- 5 个组件（`lidar_preprocessor` / `ground_segmenter` / `cylinder_detector` / `cluster_extractor` / `building_spot_analyzer`）是**同一套代码的另一份**，都是 `rclcpp::Node` 子类，且**没有任何 launch 启动它们**
- 两边**重复发布同名话题**，QoS 还不一样
- 组件的 `GroundSegmenter::validate_plane_normal` / `refine_inliers_adaptive` / `classify_by_platform` / `normalize_coefficients` 与单体内 869–935 行**同名同逻辑** —— 确认是复制，不是两套演化

---

## 任务 0：录基准 bag ⭐ 先做这个

**目标**：为后续每一步提供"改前"的对照物。没录这个，后面所有验收都无从谈起。

**前置**：`colcon build` 通过；`bringup_gazebo` 能起来（仿真链路已修好 QoS）。

**步骤**：

```bash
# 终端 1
source /opt/ros/humble/setup.bash && source ~/ws_livox/install/setup.bash
source ~/br_perception/install/setup.bash
ros2 launch br_perception bringup_gazebo.launch.py

# 终端 2 — 等 sensor_rig 开始巡游后再开录
source /opt/ros/humble/setup.bash && source ~/br_perception/install/setup.bash
mkdir -p ~/br_perception/test/test_data
ros2 bag record -o ~/br_perception/test/test_data/baseline \
  /livox/lidar/pointcloud \
  /perception/lidar/ground /perception/lidar/non_ground \
  /perception/lidar/pillars /perception/lidar/obstacles \
  /perception/lidar/building_spots
# 巡游跑完 2 圈（默认 --side 2.0 --laps 2）后 Ctrl-C
```

**验收**：`ros2 bag info` 显示 6 个话题都有数据；`/livox/lidar/pointcloud` 帧数 ≈ 时长 × 33Hz。

**产出**：`test/test_data/baseline/`（`.db3` + `metadata.yaml`）

> ⚠️ **不要提交到 git** —— bag 体积大。在 `.gitignore` 里加 `test/test_data/`。

---

## 任务 1：`lidar_algorithms` 骨架 + 搬迁 `LidarPreprocessor`

**目标**：建立算法库骨架，并用第一个（也是最小、最上游的）算法验证这个模式跑得通。

**输入**：
- 设计文档 §4（算法类接口规范）
- 参考实现：`src/lidar/lidar_preprocessor.cpp`（395 行，组件版）
- 单体里的对应实现：`src/lidar/lidar_perception_node.cpp` 的预处理段（体素降采样 / 统计离群 / ROI 裁剪 / 自过滤）

**产出文件**：

```
src/lidar/include/br_perception/lidar/algorithms/lidar_preprocessor.hpp    ← 新建
src/lidar/algorithms/lidar_preprocessor.cpp                                ← 新建
CMakeLists.txt                                                             ← 加库目标
```

**具体要求**：

1. 类形如设计文档 §4 的 `GroundSegmenter` 范式：
   ```cpp
   namespace br_perception::lidar {
   using CloudPtr = pcl::PointCloud<pcl::PointXYZI>::Ptr;
   class LidarPreprocessor {
   public:
     struct Params {
       double leaf_size{0.05};
       bool   enable_outlier_filter{true};
       int    outlier_mean_k{10};
       double outlier_std_thresh{1.0};
       double roi_min_x{-10.0}, roi_max_x{30.0};
       double roi_min_y{-10.0}, roi_max_y{10.0};
       double roi_min_z{-0.5},  roi_max_z{2.0};
       bool   enable_self_filter{true};
       double self_radius_x{0.0}, self_radius_y{0.0}, self_radius_z{0.0};
       bool   enabled{true};
     };
     explicit LidarPreprocessor(const Params& p);
     CloudPtr run(const CloudPtr& in) const;     // 纯函数: 不订阅/不发布/不碰 Node
   };
   }
   ```
2. **算法体原样搬**，不要"顺手优化" —— 本次重构的验收标准是行为不变。
3. CMake 加：
   ```cmake
   add_library(lidar_algorithms SHARED src/lidar/algorithms/lidar_preprocessor.cpp)
   ament_target_dependencies(lidar_algorithms rclcpp sensor_msgs pcl_conversions)
   target_link_libraries(lidar_algorithms ${PCL_LIBRARIES})
   ```
   并把 `src/lidar/include` 已在 `include_directories` 里（确认一下）。
4. **单体此时先不动** —— 这一步只让新库能编出来。

**验收**：
- `colcon build` 通过，`install/br_perception/lib/liblidar_algorithms.so` 存在
- 写一个临时 `main` 或直接进任务 2 再测（本任务不强制单测）

**回退**：`git revert` 本任务的提交。

---

## 任务 2：单测骨架 + `LidarPreprocessor` 单测

**目标**：建立 §11.1 要求的单测模式，并覆盖刚搬出来的第一个算法。

**产出**：`test/test_lidar_preprocessor.cpp`（新建）+ CMake 测试目标

**具体要求**：
1. CMake：
   ```cmake
   if(BUILD_TESTING)
     find_package(ament_cmake_gtest REQUIRED)
     ament_add_gtest(test_lidar_preprocessor test/test_lidar_preprocessor.cpp)
     target_link_libraries(test_lidar_preprocessor lidar_algorithms)
     ...
   endif()
   ```
2. 测试用例（**用任务 0 的 bag 作输入**，从 `.db3` 读或用 `ros2 bag play` + 订阅）：
   - ROI 裁剪：构造 ROI 外的点，断言被裁掉
   - 体素降采样：断言输出点数 < 输入点数，且包围盒不变
   - 离群滤波开关：`enable_outlier_filter` true/false 输出不同
   - **纯函数性**：同样输入两次调用结果完全一致
3. 测试**不依赖运行中的 ROS 节点**（算法类不碰 ROS，这就是它存在的意义）

**验收**：`colcon test --packages-select br_perception` 中该用例通过。

---

## 任务 3：把 `LidarPreprocessor` 接进单体，删掉重复实现

**目标**：第一次真正消除重复 —— 单体改调算法类。

**输入**：`src/lidar/lidar_perception_node.cpp` 的预处理段

**具体要求**：
1. 单体加成员 `std::unique_ptr<LidarPreprocessor> preprocessor_;`
2. 构造里把已 `declare_parameter` 的值填进 `LidarPreprocessor::Params`（**不要改参数名**，见附录 A）
3. 预处理段的方法体改为调用 `preprocessor_->run(cloud)`
4. **删掉单体内那份重复的预处理实现**

**验收**：
```bash
ros2 bag play ~/br_perception/test/test_data/baseline
# 另开终端
ros2 launch br_perception bringup_lidar_only.launch.py    # 或 bringup_gazebo
```
逐话题对比新输出与 bag 里录的旧输出，判定标准见设计文档 §六阶段 2–6：
**首选 bit 级一致**；允差时点数差 ≤0.1%、质心差 ≤1mm；**分类标签必须 100% 一致**。

**回退**：`git revert`。

---

## 任务 4：搬迁 `GroundSegmenter`

**目标**：把地面分割从组件搬成算法类，单体改调它。

**输入**：
- 组件版：`src/lidar/ground_segmenter.cpp`（539 行）：`fit_ground_plane` / `validate_plane_normal` / `refine_inliers_adaptive` / `extract_ground_and_non_ground` / `classify_by_platform` / `normalize_coefficients`
- 单体版：`src/lidar/lidar_perception_node.cpp` 536–604 行（RANSAC → 法向验证 → 动态阈值精筛 → 分离地面/非地面 → 分层）+ 869–935 行

**产出**：
```
src/lidar/include/br_perception/lidar/algorithms/ground_segmenter.hpp
src/lidar/algorithms/ground_segmenter.cpp
test/test_ground_segmenter.cpp
```

**具体要求**：
1. 接口按设计文档 §4 的 `GroundResult`：返回 `ground` / `non_ground` / `l0_surface` / `l1_surface` / `l2_surface` / `plane` / `elapsed_ms`
2. `Params` 含：`ransac_dist_thresh` / `ransac_max_iters` / `ransac_eps_angle` / `enable_normal_constraint` / `normal_angle_threshold` / `l0_height` / `l1_height` / `l2_height` / `layer_tolerance` / `enabled`
   - ⚠️ **参数名以 `config/lidar_params.yaml` 现有键名为准**（见附录 A），不要用组件那份的长名
3. 单测：平面拟合精度、法向量方向正确性、地面/非地面分离正确率、分层高度正确性

**验收**：同任务 3 的逐话题对比，重点看 `/perception/lidar/ground`、`/perception/lidar/non_ground`、`/perception/lidar/l0|1|2_surface`、`/perception/lidar/ground_plane`。

---

## 任务 5：搬迁 `ClusterExtractor`

**输入**：
- 组件版：`src/lidar/cluster_extractor.cpp`（806 行）
- 单体版：`src/lidar/lidar_perception_node.cpp` 圆柱掩膜段（~752）+ 欧式聚类段（~770）+ 障碍物构建/过滤/分类（~784）+ `classify_obstacle`（1000）

**产出**：`algorithms/cluster_extractor.{hpp,cpp}`、`test/test_cluster_extractor.cpp`

**具体要求**：
1. 返回 `std::vector<Obstacle>` 的**算法内部结构**（不是 ROS msg —— msg 组装留在薄壳里）
2. `Params` 含：`cluster_tolerance` / `min_cluster_size` / `max_cluster_size` / `max_object_size` / `max_object_height` / `min_object_points` / `floating_z_min` / `size_tolerance` / `earth_cube_size` / `sky_cube_size` / `enemy_robot_size` / `mustika_size` / `elongation_ratio` / `sphere_eigen_tol` / `enable_cylinder_mask` / `cylinder_mask_radius` / `enabled`
3. 单测：不同距离下的聚类精度、尺寸过滤正确性、分类标签正确性

**验收**：重点看 `/perception/lidar/obstacles`，**分类标签必须 100% 一致**。

---

## 任务 6：搬迁 `CylinderDetector`

**输入**：
- 组件版：`src/lidar/cylinder_detector.cpp`（779 行）：水平切片 + 2D 圆拟合、3D RANSAC 圆柱
- 单体版：`lidar_perception_node.cpp` 605–770 行

**产出**：`algorithms/cylinder_detector.{hpp,cpp}`、`test/test_cylinder_detector.cpp`

**具体要求**：
1. `Params` 含：`normal_k_search` / `slice_thickness` / `slice_min_points` / `circle_fit_thresh` / `circle_fit_iters` / `radius_min` / `radius_max` / `merge_center_tol` / `merge_radius_tol` / `merge_min_slices` / `min_cyl_height` / `cyl3d_*` / `mustika_h_min|max` / `core_h_min|max` / `top_min_pts` / `top_cluster_tol` / `top_z_range` / `top_z_offset` / `geo_check_enabled` / `geo_match_tol` / `normal_omp_threads` / `min_height` / `enabled`
2. ⚠️ 注意：`normal_omp_threads` 与 OpenMP —— 目标机 6C12T，别开满，见附录 B
3. 单测：不同高度/直径圆柱的检测率、误检率；柱顶窗口（`top_z_*`）行为

**验收**：重点看 `/perception/lidar/pillars`。

---

## 任务 7：搬迁 `BuildingSpotAnalyzer`

**输入**：
- 组件版：`src/lidar/building_spot_analyzer.cpp`（641 行）
- 单体版：`lidar_perception_node.cpp` 建筑位分析段（~133 参数区 + 调用处）

**产出**：`algorithms/building_spot_analyzer.{hpp,cpp}`、`test/test_building_spot_analyzer.cpp`

**具体要求**：
1. 建筑位坐标 **从 `config/field_geometry.yaml` 读**（10 个位：L1 六个 z=0.6 + L2 四个 z=0.9），不要在算法类里硬编码
2. `Params` 含：`platform_z` / `spot_half_size` / `column_z_min|max` / `min_points_threshold` / `top_surface_tolerance` / `top_surface_min_points` / `empty_max_height` / `one_earth_min|max` / `two_earth_min|max` / `complete_tower_min|max` / `enabled`
   - ⚠️ 单体里用的是 `top_surface_min_pts`，组件里是 `top_surface_min_points` —— **统一成一个**（见附录 A）
3. 单测：用合成点云验证 空 / 1地球 / 2地球 / 完整塔 四态判定，以及塔顶颜色

**验收**：重点看 `/perception/lidar/building_spots`，**状态与塔顶颜色必须 100% 一致**。

---

## 任务 8：清理组件外壳

**目标**：5 个算法都搬完后，删掉重复的 Node 外壳。

**具体要求**：
1. 删除 `src/lidar/{lidar_preprocessor,ground_segmenter,cylinder_detector,cluster_extractor,building_spot_analyzer}.cpp` 的 **Node 外壳部分**（注意：源码文件里的算法体已在任务 1/4/5/6/7 搬走，此时这几个文件只剩壳）
2. 删除对应的 `src/lidar/include/br_perception/lidar/*.hpp`（除了 `lidar_perception_node.hpp`）
3. CMakeLists 清理：
   - 删掉 §1–§5 的 `add_lidar_component(...)` 调用
   - 从 `install(TARGETS ...)` 的组件库列表里删掉对应的 `*_component`
   - 从 2026-09-14 新加的"注册节点可执行" `install(TARGETS ...)` 里删掉这 5 个名字
4. **保留** `lidar_perception_node`（它是容器 A 的薄壳）

**验收**：`colcon build` 通过；`ros2 pkg executables br_perception` 里不再出现那 5 个名字；管线行为不变（再对一次 bag）。

---

## 任务 9：容器化

**目标**：按设计文档 §3.1 把容器 A 拆出来。

**产出**：`src/lidar/lidar_container.cpp`（新建）+ `launch/bringup_*.launch.py` 改造

**具体要求**：
1. 容器用 `rclcpp_components` 的 `component_container`；`lidar_pipeline` 作为组件注册进去
2. 三条流水线各一个容器（雷达 / 视觉 / 融合）；视觉与融合此时可以是空壳，先把雷达的跑通
3. **不启用 `use_intra_process_comms`** —— 见设计文档 §3.2 决策 3，容器内是函数调用，用不上
4. 线程/核绑定按设计文档 §3.1 的表（雷达 core 0–1；Point-LIO 单独 core 5）。可用 `taskset` 起步，`chrt` 实时调度留到后面
5. 老 launch 保留一个 `bringup_lidar_only`（非容器）作为对照，方便 A/B

**验收**：
- `ros2 component list` 能看到 `lidar_pipeline`
- 话题输出与任务 8 的验收结果一致
- 记录 CPU 占用（`top -H`）与端到端延迟，作为 §6.1 性能预算的基线

---

## 任务 10：消息接口

**目标**：落地设计文档 §5 —— 把 `DiagnosticArray`/`MarkerArray` 当数据接口的现状换掉。

**产出**：
```
msg/LidarResult.msg        ← 新建
msg/PerceptionFrame.msg    ← 修改
src/lidar/lidar_perception_node.cpp  ← 发布 LidarResult
CMakeLists.txt             ← rosidl_generate_interfaces 加 LidarResult.msg
```

**具体要求**：
1. 新增 `LidarResult.msg`：
   ```
   std_msgs/Header header          # frame_id = "world"
   Obstacle[] obstacles
   BuildingSpot[] building_spots
   PillarStatus mustika_pillar
   PillarStatus core_pillar
   float32[] stage_elapsed_ms
   bool watchdog_ok
   ```
2. `PerceptionFrame.msg` 按设计文档 §5.2 的表改动（Header、`seq` 改名、`PoseStamped`、补 5 个 §5.1 字段、`mustika_state` 注释补 6）
3. `MarkerArray` / `debug_cloud` **保留但只作可视化**，不再承担数据职责
4. 检查 `obstacles` / `building_spots` 的 `MarkerArray` 与 `Obstacle` / `BuildingSpot` msg **字段是否对得上**（`bbox_size`、`velocity`、`confidence`、`source` 在 Marker 里可能没有对应）

**验收**：`ros2 topic echo /perception/lidar/result` 能看到结构化字段；`colcon build` 通过。

---

## 任务 11：`fusion_node` 进容器 C

**目标**：端到端打通 `LidarResult` → `PerceptionFrame`。

**注意事项**：
- `coordinate_transformer` 已实现（`src/fusion/coordinate_transformer.cpp`），是**纯辅助类**（构造接 `rclcpp::Node*`），不是组件
- 里程计话题是 `/aft_mapped_to_init`（Point-LIO ROS2 移植版），**不是** `/Odometry`
- `config/fusion_params.yaml` 里 `odometry.*` 一节是它的参数；⚠️ 该文件第 104 行的 `static_tf:` 块**目前没有任何代码读**，外参实际写死在 `launch/bringup_full.launch.py` 里 —— 本次要么让 launch 读它，要么删掉，别继续留着两份
- 仿真下 **`coordinate_transformer` 的 TF 广播**与 `sensor_rig` 的 `planar_move`（`publish_odom_tf: true`）会抢发同一条 `odom→base_link`，需要一个 `broadcast_tf` 开关

**验收**：`ros2 topic echo /perception/output` 有 `PerceptionFrame`，`header.frame_id == "world"`。

---

## 附录 A：参数名冲突与缺失清单

**规则：以 `config/lidar_params.yaml` 现有键名优先；yaml 里没有的，用组件那份的名字。**

已确认的冲突/缺失（2026-09-14 核对）：

| 单体用的名字 | 组件用的名字 | 现状 |
|---|---|---|
| `top_surface_min_pts` | `top_surface_min_points` | 两者并存，yaml 只有后者 |
| `ransac_dist_thresh` | `ransac_distance_threshold` | 两者并存，yaml 只有前者 |
| `ransac_max_iters` | `ransac_max_iterations` | 两者并存，yaml 只有前者 |
| — | `enable_self_filter` / `self_radius_x` / `self_radius_y` / `self_radius_z` | **两份 yaml 都没有**，靠代码默认值 |
| — | `enable_cylinder_mask` / `cylinder_mask_radius` | **两份 yaml 都没有** |
| — | `circle_fit_iters` / `geo_check_enabled` / `geo_match_tol` / `min_height` / `normal_omp_threads` / `top_cluster_tol` / `top_min_pts` / `top_z_offset` / `top_z_range` | **两份 yaml 都没有** |

> 完整核对方法（可复现）：把 `src/lidar/*.cpp` 里所有 `declare_parameter<...>("...")` 与 `GET("...")`
> 的名字取出来，与 `config/lidar_params.yaml` + `config/field_geometry.yaml` 的**扁平化**键名（ROS 2 会把嵌套 map
> 变成 `a.b` 点号名）求差集。

## 附录 B：其它已知项

1. **`normal_omp_threads` 与 OpenMP**：目标机 6C12T，但雷达线程只占 core 0–1，OpenMP 别开满（建议 ≤2），
   否则会和视觉线程抢核。
2. **`src/camera/` 有 4 个检测器但缺 `camera_perception_node`**（§15.1 模块 10 未开工），
   且 `config/camera_params.yaml` **没有**这 4 个检测器的参数块（只有相机内参/外参）。
   相机层容器化时（不在本次范围）要一并补齐。
3. **单体与组件都是"两套实现"**，本次重构完成后应只剩一套。若发现还有第三处复制，报出来。
4. 本次重构**不动** Point-LIO 及其配置（`config/pointlio_gazebo.yaml` 是仿真版，实机版在 `~/lio_ws/`）。
